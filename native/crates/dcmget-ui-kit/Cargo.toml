[package]
name = "dcmget-ui-kit"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DcmGet desktop presentation contracts and GPUI component adapters"

[features]
default = []
gpui = ["dep:gpui", "dep:gpui-component"]

[dependencies]
serde.workspace = true
gpui = { version = "=0.2.2", optional = true, features = ["runtime_shaders"] }
gpui-component = { version = "=0.5.1", optional = true }

[lints]
workspace = true
