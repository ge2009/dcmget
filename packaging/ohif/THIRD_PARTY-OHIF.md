# OHIF Viewer third-party notice

DcmGet redistributes the compiled `package/dist` web application from the
official [`@ohif/app` 3.12.6 npm package](https://www.npmjs.com/package/@ohif/app/v/3.12.6).
The package identifies its primary license as MIT; the accompanying
`LICENSE-OHIF.txt` is copied verbatim from the pinned npm archive.

OHIF Viewer is built from multiple open-source packages whose licenses remain
applicable to their respective code. The upstream source and dependency
information are available in the
[`OHIF/Viewers` v3.12.6 source tree](https://github.com/OHIF/Viewers/tree/v3.12.6).
The source archive URL, byte size and SHA-256 used by DcmGet are recorded in
`ohif-3.12.6.json` and in each prepared payload's provenance file.
